import { View, Text, ScrollView } from "react-native";
import React from "react";

const OpenAiTab = () => {
  return (
    <ScrollView
      className="bg-surface"
      contentInsetAdjustmentBehavior="automatic"
    >
      <Text className="text-white">OpenAiTab</Text>
    </ScrollView>
  );
};

export default OpenAiTab;
