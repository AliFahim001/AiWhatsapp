import { View, Text, ScrollView } from "react-native";
import React from "react";

const PhoneTab = () => {
  return (
    <ScrollView
      className="bg-surface"
      contentInsetAdjustmentBehavior="automatic"
    >
      <Text className="text-white">PhoneTab</Text>
    </ScrollView>
  );
};

export default PhoneTab;
